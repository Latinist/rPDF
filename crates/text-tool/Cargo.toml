[package]
name = "text-tool"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
docx-core = { version = "0.1.0", path = "../docx-core" }
shared = { path = "../../shared" }
